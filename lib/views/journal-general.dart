import 'package:flutter/material.dart';

class JVPage extends StatelessWidget {
  const JVPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('JV Page')),
      body: const Center(child: Text('JV Page Content')),
    );
  }
}
